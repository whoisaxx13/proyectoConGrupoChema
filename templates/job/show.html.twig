{% extends 'base.html.twig' %}

{% block title %}Job{% endblock %}


    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ job.description }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ job.time }}</td>
            </tr>
        </tbody>
    </table> #}


    {% block body %}
    <h1 class="mb-auto text-center font-italic Italica">Trabajo</h1>
    <form>
        <div class="mb-1 form-group">
            <label for="descriptionInput" class="text-secondary">Descripción:</label>
            <input type="text" id="descriptionInput"   class="form-control" name="descriptionInput">
        </div>
        <div>
            <label for="timeInput" class="text-secondary">Tiempo:</label>
            <input type="number"  id="timeInput" class="mb-2  form-control" name="timeInput">
        </div>
        <button type="submit" class="mb-2  btn btn-primary">Enviar</button>
         <a href="{{ path('app_job_index') }}"  class="mb-2 btn btn-secondary" >Volver a la lista</a>
            {{ include('job/_delete_form.html.twig') }}
    </form>
    {# <a href="{{ path('app_job_edit', {'id': job.id}) }} class="btn btn-warning" ">Editar</a> #}
{% endblock %}

